management:
    metrics:
        distribution:
            slo:
                "http.server.requests": 50ms,100ms,250ms,500ms,1s,2s,5s,10s
                "spring.data.repository.invocations": 10ms,25ms,50ms,100ms,250ms,500ms,1s,2s,5s

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/concert_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                format_sql: true
